export enum TransactionTypeEnum {
  TASK_REWARD = "task_reward",
  REFERRAL_BONUS = "referral_bonus",
  LOTTERY_ENTRY = "lottery_entry",
  SPINNER_REWARD = "spinner_reward",
  TREE_SHAKE_REWARD = "tree_shake_reward",
  GAME_LEADERBOARD_REWARD = "game_leaderboard_reward",
  SWAP = "swap",
  GAME_ENTRY = "game_entry",
}

export enum TransactionStatusEnum {
  PENDING = "Pending",
  COMPLETED = "Completed",
  FAILED = "Failed",
  EXPIRED = "Expired",
}
